import 'package:flutter/material.dart';

class ClickToChat extends StatelessWidget  {
  const ClickToChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
